#include "filter.hpp"

namespace projector {

bool filter::check_particle(const particle &p) const {

    // check source

    // check geom

    // check interaction

    // check element

    // check range
    return true;
}

bool filter::check_particle_segment(const particle &p, const std::size_t segment,
                                    const vec3 position) const {
    // check source

    // check geom

    // check interaction

    // check element

    // check range
    return true;
}

} // namespace projector